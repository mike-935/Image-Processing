package controller;

/**
 * Represents a controller for an ImageProcessing Functionality that is provided from
 * an ImageProcessing model and a view that provides viewing functionality.
 */
public interface ImageProcessingController {

  /**
   * Runs this controller to allow for various commands to be performed to an image based on
   * given user inputs.
   */
  void run();
}
